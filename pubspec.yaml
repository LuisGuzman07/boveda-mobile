name: boveda_mobile
description: "Bóveda Authenticator — Aplicación móvil de autenticación en dos pasos"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cupertino_icons: ^1.0.8
  crypto: ^3.0.6
  shared_preferences: ^2.3.2
  mobile_scanner: ^7.4.2
  cryptography: ^2.9.0
  flutter_secure_storage: ^9.2.4
  local_auth: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
